import { Container, Typography } from '@mui/material';
export default function LoginPage() {
  return (
    <Container sx={{ py: 4 }}>
      <Typography variant="h4">Detalle Page</Typography>
      <Typography color="text.secondary">Formulario próximamente</Typography>
    </Container>
  );
}